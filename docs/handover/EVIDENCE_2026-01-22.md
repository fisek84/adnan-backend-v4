# Evidence — 2026-01-22

This document captures the exact local verification outputs requested.

## pre-commit (all files)

Command:

```bash
pre-commit run -a
```

Output:

```text
ruff (lint)..............................................................Passed
ruff (format)............................................................Passed
mypy (local gate via mypy_local.ini).....................................Passed
```

## pytest

Command:

```bash
pytest -q
```

Output:

```text
................................s.........................................................................................................................ss................ [ 99%]
.                                                                                                                                                                            [100%] 
170 passed, 3 skipped in 121.61s (0:02:01)
```

## Notion runtime budgets (env)

These env vars are read at runtime by the Notion IO layer.

| Env var | Meaning | Default |
|---|---|---:|
| `CEO_NOTION_MAX_CALLS` | Max number of Notion HTTP calls allowed within a single read operation scope (budget context). | 2 |
| `CEO_NOTION_MAX_LATENCY_MS` | Max total elapsed time budget for Notion read operation scope (soft-fail when exceeded). | 1500 |
